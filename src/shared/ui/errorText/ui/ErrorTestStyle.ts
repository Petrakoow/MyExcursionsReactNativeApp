import {StyleSheet} from 'react-native';
import {palette} from '@/shared/config/colors';
export const styles = StyleSheet.create({
    container: {},
    titleError: {
        color: palette.light.error,
    },
    descriptionError: {
        color: palette.light.errorSecondary,
    },
});
